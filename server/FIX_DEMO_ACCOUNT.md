### احذر:
يجب تشغيل الأوامر من مجلد `server`.

### الحل:
يجب تشغيل أمر seed لإنشاء قاعدة البيانات والحساب التجريبي:

### في Terminal الخاص بالـ Backend:

1. **أوقف السيرفر** (اضغط Ctrl+C)

2. **شغل أمر seed:**
```bash
npm run seed
```

يجب أن ترى هذه الرسالة:
```
✅ Database seeding completed successfully!

📝 Demo User Credentials:
   Email: admin@store.com
   Password: admin123
```

3. **شغل السيرفر مرة أخرى:**
```bash
npm run dev
```

---

## إذا استمرت المشكلة:

### إعادة تعيين قاعدة البيانات:
(ملاحظة: هذا سيحذف جميع البيانات الحالية)

```bash
# في مجلد server
node scripts/cleanup_db.js
npm run seed
npm run dev
```

---

## ملاحظة مهمة:

أمر `npm run seed` يعمل **مرة واحدة فقط** لإنشاء البيانات الأولية. إذا كانت البيانات موجودة بالفعل، سيتخطى الأمر ويقول "Database already seeded".
